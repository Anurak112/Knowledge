# Tools

This directory contains definitions for tools that agents can use. Tools can be native functions, API wrappers, or external scripts.

## Structure

-   **`custom/`**: User-defined tools.
-   **`wrappers/`**: Wrappers for existing libraries or APIs.

## Format

Tool definitions usually include the schema (parameters, description) and implementation path.
