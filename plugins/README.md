# Plugins

Plugins are packages that extend the functionality of the agent runtime. They can include sets of tools, skills, and configuration overrides.

## Structure

```
plugins/
└── plugin-name/
    ├── manifest.json   # Plugin metadata
    └── src/            # Source code
```
