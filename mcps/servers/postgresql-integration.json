{
    "name": "postgresql",
    "description": "Connect to PostgreSQL databases for advanced data operations. เชื่อมต่อฐานข้อมูล PostgreSQL สำหรับการจัดการข้อมูลขั้นสูง",
    "command": "npx",
    "args": [
        "-y",
        "@modelcontextprotocol/server-postgres"
    ],
    "env": {
        "POSTGRES_CONNECTION_STRING": "postgresql://user:password@localhost:5432/dbname"
    }
}